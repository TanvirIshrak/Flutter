import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke test - grade tracking package compiles', () {
    // Intentionally empty: UI tests can be added later.
    expect(true, isTrue);
  });
}
