org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Disable Kotlin incremental compilation to avoid "Could not close incremental caches"
# failures that happen when plugin sources live on a different drive than the
# Android project (Pub cache on C:\, project on D:\).
kotlin.incremental=false
kotlin.daemon.useFallbackStrategy=true
kotlin.daemon.jvm.options=-Xmx2G
